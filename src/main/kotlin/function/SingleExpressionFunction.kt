package com.dicodingdelfi.kotlin.function

/*fun sum(x: Int, y: Int): Int {
    return x + y
}*/

// the top function can be concise to one line
fun sum(x: Int, y: Int) = x + y

fun main() {
    println(sum(1, 2))
}